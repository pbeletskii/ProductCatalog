<Window x:Class="WpfApp5.AddProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp5"
        mc:Ignorable="d"
        Title="Добавить продукт" Height="240" Width="300">
    <Grid VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Label Content="Имя:"/>
            <TextBox x:Name="productNameTextBox" Margin="5"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <Label Content="Цена:"/>
            <TextBox x:Name="priceTextBox" Margin="5"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Label Content="Описание:"/>
            <TextBox x:Name="DescriptionTextBox" Margin="5"/>
        </StackPanel>
        <Button Content="Добавить" Grid.Row="3" Click="AddButton_Click" Margin="5"/>
    </Grid>
</Window>
